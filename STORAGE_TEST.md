# 🧪 Тест сохранения данных

## Проблема

В standalone режиме данные не сохраняются. В логах нет сообщений о сохранении.

## Что проверить

### 1. Проверьте консоль браузера (если доступна)

Откройте консоль и ищите:
- `[Storage] ✅ Stored in memory:`
- `[Storage] 🔄 Saving to JUCE PropertiesFile:`
- `[JUCE Bridge] 🔄 saveDataToJuce called:`
- `[JUCE Bridge] 📤 Attempting to send message:`
- `[Helper] 🔄 Intercepted SAVE_DATA:`

### 2. Проверьте логи JUCE

**macOS:**
```
~/Library/Application Support/Sairyne/Sairyne.log
```

Ищите:
- `handleSaveDataEvent called`
- `handleSaveDataEvent: Saved data:`
- `handleCustomScheme: detected sairyne://save_data`

### 3. Проверьте файл PropertiesFile

**macOS:**
```
~/Library/Application Support/Sairyne/Sairyne.properties
```

Если файла нет, значит данные не сохраняются.

---

## 🔍 Диагностика

### Если нет сообщений `[Storage]`:
- Функции сохранения не вызываются из frontend
- Проверьте, вызывается ли `createSession()` при входе

### Если есть `[Storage]` но нет `[JUCE Bridge]`:
- `isJuceAvailable()` возвращает `false`
- Проверьте, определяется ли `window.juce.postMessage`

### Если есть `[JUCE Bridge]` но нет `[Helper]`:
- Helper script не перехватывает сообщения
- Проверьте, загружается ли helper script

### Если есть `[Helper]` но нет `handleSaveDataEvent`:
- События не доходят до JUCE
- Проверьте, работает ли `bridge.emitEvent`

---

## 📝 Следующие шаги

1. **Пересоберите JUCE проект**
2. **Протестируйте:**
   - Войдите в систему
   - Создайте проект
   - Проверьте логи
3. **Пришлите:**
   - Последние 50 строк логов JUCE
   - Сообщения из консоли браузера (если доступна)

---

**Проект задеплоен с улучшенным логированием. Теперь можно точно увидеть, где проблема! 🚀**

