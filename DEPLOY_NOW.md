# 🚀 Инструкция по деплою (без установки Vercel CLI)

## ✅ Сборка прошла успешно!

Проект собран и готов к деплою.

---

## 🚀 Вариант 1: Через npx (без установки)

Выполните команду:

```bash
cd /Users/trilium/Downloads/SairyneSignIn
npx vercel --prod
```

**При первом запуске:**
- Спросит логин → введите email и пароль от Vercel
- "Link to existing project?" → **Y** (да)
- Выберите проект **sairyne-ai**
- Деплой начнется автоматически

---

## 🚀 Вариант 2: Через веб-интерфейс Vercel

### Шаг 1: Подготовка
1. Убедитесь, что все изменения закоммичены в Git (если используете Git)
2. Или просто загрузите файлы через веб-интерфейс

### Шаг 2: Деплой через Vercel Dashboard
1. Зайдите на https://vercel.com
2. Войдите в аккаунт
3. Откройте проект **sairyne-ai**
4. Нажмите **"Deployments"** → **"Redeploy"**
5. Или нажмите **"Settings"** → **"Git"** и сделайте `git push` (если проект связан с Git)

### Шаг 3: Загрузка через веб-интерфейс (если нет Git)
1. Зайдите на https://vercel.com
2. Откройте проект **sairyne-ai**
3. Нажмите **"Settings"** → **"General"**
4. В разделе **"Deployments"** нажмите **"Upload"**
5. Загрузите папку `dist` (результат сборки)

---

## 🚀 Вариант 3: Через Git (если проект в Git)

Если проект подключен к Git (GitHub/GitLab):

```bash
cd /Users/trilium/Downloads/SairyneSignIn
git add .
git commit -m "Update: WebView compatibility, session persistence, master channel notice"
git push
```

Vercel автоматически задеплоит при push в репозиторий.

---

## ✅ Проверка после деплоя

После деплоя проверьте:

1. **Главная страница:**
   ```
   https://sairyne-ai.vercel.app
   ```

2. **Embed-chat (для JUCE):**
   ```
   https://sairyne-ai.vercel.app/embed-chat.html
   ```

3. **Проверьте в консоли (F12):**
   - Нет ошибок
   - UI загружается корректно

---

## 🔧 Проверка переменных окружения

Убедитесь, что на Vercel есть переменная:

1. Зайдите на https://vercel.com
2. Откройте проект **sairyne-ai**
3. **Settings** → **Environment Variables**
4. Должна быть: `VITE_API_URL` = `https://sairyne-full5.onrender.com`

Если нет - добавьте и передеплойте.

---

## ⚡ Рекомендуемый способ

**Самый простой:** Используйте `npx vercel --prod`

```bash
cd /Users/trilium/Downloads/SairyneSignIn
npx vercel --prod
```

Это не требует установки и работает сразу!

